# Copy to .env and fill in. The .env file is read by dotenv and is gitignored.
# Get a key at https://console.anthropic.com/ — use a low-limit, revocable key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Port the relay listens on (default 3000).
PORT=3000
